import React from 'react'

function About() {
  return (
    <div>
        <h1>About</h1>
    </div>
  )
}

export default About

// its popular library handles navigation in react application.
// enables creation of single page application with multiple views

// it's define routes, associate them with specific component 
